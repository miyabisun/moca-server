{
  "name": "moca-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun src-ts/index.ts",
    "dev": "cd client && bunx vite build --watch & bun --watch src-ts/index.ts",
    "build:client": "cd client && bun install && bun run build",
    "build": "bun run build:client",
    "test": "bun test"
  },
  "dependencies": {
    "drizzle-orm": "^0.39.0",
    "hono": "^4.7.2"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.3"
  }
}
